# Constants defined more than once in the compile set WITH DIFFERENT VALUES, which
# scripts/check-lib-symbols.py would otherwise fail the build on.
#
# An entry here is a DEFERRAL, not a dispensation: it means the divergence is known,
# filed upstream, and understood to be unreachable from this repo's src/. Every line
# must name the upstream issue. Delete the line when the fix lands — the gate then
# starts protecting the name again.
#
# ⚠ Do NOT add a name here to make a red build green. The one time this ecosystem
# allowed a lib/ exception on trust (check-clean.sh's `sakshi.cyr` allowance,
# 2026-08-05 to 2026-08-07) it silently reverted a fix for two days.
#
#   <NAME>   # <why it is safe here> — <upstream issue>
