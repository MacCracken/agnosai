# 018 — sankoch's path check makes `.agpkg` stricter than the oracle

## Status: Accepted

## Context

`rust-old/src/definitions/packaging.rs` reads and writes `.agpkg` ZIP bundles
through the `zip` crate. The Cyrius port reads and writes them through
`lib/sankoch.cyr`, and the two libraries disagree about which member names are
allowed. The disagreement is not a bug in either: it is one library taking a
position the other leaves to its caller.

**The oracle's only filter is one substring test, applied on import, to the
definitions scan only** (`packaging.rs:127`):

```rust
if name.contains("..") { return None; }
```

It sits inside a `filter_map`, so a matching entry is a **silent skip** — the
import still returns `Ok`, with that definition absent. Nothing filters names on
export: `format!("definitions/{}.json", def.agent_key)` (`:70`) interpolates the
key raw.

**sankoch applies `_zip_path_safe` on both sides** — inside `zip_add_meta` /
`zip_add_any_meta` (write) and inside `_zip_prepare`, which every read path
funnels through (read) — and returns `ERR_UNSAFE_PATH` rather than skipping. It
rejects a `..` *component*, a leading `/`, a backslash, a NUL or control byte,
and an empty interior component.

> ⚠ **sankoch is cited by function, not by line.** It is a **stdlib fold**, not
> a `[deps.*]` git pin: `lib/sankoch.cyr` is regenerated by `cyrius lib sync`
> and every line number in it moves on a toolchain bump. The first draft of this
> ADR cited five, and by 2026-08-11 every one of the five was wrong. The version
> in the tree at that re-check is **sankoch 2.7.7** under cyrius 6.5.18.

The two filters differ **in both directions**:

| name | oracle | sankoch |
|---|---|---|
| `definitions/a..b.json` | skipped (substring `..`) | accepted (no `..` component) |
| `definitions/../x.json` | skipped | rejected |
| `definitions/a\b.json` | **read and parsed** | rejected |
| `definitions//b.json` | **read and parsed** | rejected |
| `definitions/\x01.json` | **read and parsed** | rejected |

Three of these are reachable only from a hostile or hand-built archive; the
export side is reachable from ordinary code, because an `agent_key` of `../x`
produces `definitions/../x.json`.

## Decision

**Reproduce the oracle's filter exactly, and let sankoch's stand where it fires
past it.**

1. `_agnosai_pkg_is_definition_member` applies the oracle's substring `..` test
   as a **silent skip**, including its false positives (`a..b.json`). The port
   drops exactly what the oracle drops.
2. A name that survives that and then trips sankoch's check surfaces as an
   error rather than being read:
   - on **import**, `agnosai_error_io` — the same variant the oracle raises when
     its own `read_to_string` fails on an unreadable member;
   - on **export**, `agnosai_error_other("zip error: …")` — the same variant the
     oracle raises when `start_file` refuses a name.
3. sankoch's check is **not bypassed**. There is no unchecked extract entry
   point, and adding one to chase parity would mean carrying a zip-slip primitive
   in the tree for the sake of reading a member whose name is already malformed.

Five further differences fall out of the same library swap and are accepted with
it, all in the safe direction:

4. **The 1 MiB per-file guard is real here and advisory in the oracle.**
   `file.size()` is attacker-written central-directory metadata and the `zip`
   crate never enforces it against the decompressed stream, so a bomb declaring
   `uncompressed_size = 10` passes the oracle's guard and then inflates
   unbounded into RAM. sankoch's `zip_extract` bounds the output at `dst_cap`
   and `_zip_verify` requires the produced length to equal the declared one
   *and* the CRC-32 to match, so the declared size is the real ceiling.
5. **Export is byte-reproducible here and is not upstream.** zip 8's
   `SimpleFileOptions::default()` stamps `OffsetDateTime::now_utc()` into every
   header, so two exports of the same package differ. `zip_add` passes
   `mtime = 0`, so two exports here are identical.
6. **An encrypted member fails the whole import.** `zip_open` rejects an archive
   with general-purpose bit 0 set on any member, where zip 8 opens it and the
   oracle's `by_index(i).ok()?` silently skips just that entry. An archive mixing
   a valid manifest with an encrypted member is adversarial in every realistic
   case and sankoch's refusal is loud.

   ⚠ **Amended 2026-08-11.** When this was written, `zip_open` answered a bare 0
   for *every* failure — truncated, multi-disk, bad central directory, encrypted,
   OOM — so the port had nothing to branch on and collapsed all of them into one
   `InvalidDefinition`. That was filed and sankoch 2.7.7 answered with
   `zip_last_error()`. `agnosai_package_import` now consumes it and maps to the
   **oracle's** variant taxonomy, not sankoch's: corrupt/truncated →
   `InvalidDefinition` ("not a readable archive"); encrypted or multi-disk →
   `InvalidDefinition` too, but with its own sentence, because the archive is
   well-formed and a caller reading "invalid zip" would go hunting for corruption;
   OOM → `Io`, because it is not a statement about the bytes. The divergence
   from the oracle is unchanged — it would open this archive and skip one entry —
   but it is now **legible** instead of indistinguishable from corruption.
7. **Three compression methods are skipped rather than read.** sankoch decodes
   store, deflate, bzip2, zstd and xz; zip 8's default features add deflate64,
   LZMA and PPMd. `_agnosai_pkg_can_decode` turns an undecodable member into the
   oracle's own *silent skip* (`by_index(i).ok()?`) rather than an error, so the
   shape matches even where the set does not. `_agnosai_pkg_read_member` reads
   through `zip_extract_any`, not `zip_extract`, so bzip2/zstd/xz members are
   read rather than refused — the oracle's zip 8 carries those decoders and a
   narrower reader here would have been a divergence of its own.

8. **A member written THROUGH a symlink another member declares is refused.**
   Added by sankoch 2.6.4, and added to *this* document on 2026-08-11.
   ⚠ Whether it was already in the fold when the seven points above were written
   cannot be determined — `lib/` is gitignored, so there is no history of which
   sankoch the tree carried on any given day — and the original text does not
   mention it either way. It is recorded here as a difference that is true now,
   not as one that was overlooked then. `_zip_prepare` consults an
   archive-global symlink ledger and returns
   `ERR_UNSAFE_PATH` for any non-symlink member whose directory prefix is a
   symlink declared *anywhere* in the archive, regardless of index order. No
   per-entry name check can see that escape, and the oracle has no equivalent —
   zip 8 hands the caller the bytes and the mode bits and leaves the decision to
   them. `.agpkg` import reaches it through `zip_extract_any`, so it is live
   here. It cannot fire on anything this port writes: `export` emits
   `definitions/<key>.json` and `manifest.json` through `zip_add`, which passes
   `mode = 0` and therefore never sets `S_IFLNK`.

## Consequences

- An archive whose definition member names are malformed in a way the oracle
  tolerates fails to import, loudly, instead of yielding definitions. The
  affected names cannot be produced by this port's own `export`, and the
  behaviour is a refusal rather than a silent difference.
- An `agent_key` containing a `..` path component, a backslash or a control byte
  makes `export` fail. Upstream it exports and then silently loses that
  definition on the way back in — the oracle's own substring filter drops
  `definitions/../x.json`. A loud failure at write time is the better half of
  that trade, and it is the only one available.
- `.agpkg` files are not byte-compatible between the Rust build and this one —
  pretty-printer differences, metadata key order (serde_json sorts, bayan
  preserves insertion order) and the timestamp above. They remain **format**
  compatible: each side reads what the other writes.
- `tests/definitions_packaging.tcyr` pins every row of the table above, so a
  future sankoch that relaxes or tightens `_zip_path_safe` shows up as a test
  failure rather than a behaviour change.

## Re-check — 2026-08-11, post-port

**The decision holds and is what `src/definitions/packaging.cyr` does.**
`_agnosai_pkg_is_definition_member` is still a `str_contains_cstr(s, "..")`
substring test returning a silent skip, with the `definitions/` prefix and
`.json` suffix tests after it; `_agnosai_pkg_read_member` still maps a negative
`zip_extract_any` — `ERR_UNSAFE_PATH` included — to `agnosai_error_io`; export
still maps a refused name to `agnosai_error_other("zip error: …")`; and there is
still no unchecked extract entry point.

**Two things moved, both because sankoch did, and both are amended above:** the
line numbers (now cited by function — see the note in Context) and the
`zip_open` failure taxonomy (point 6). A third, sankoch's symlink ledger, is new
since the original text and is added as point 8.

✅ **SEVEN line-number citations into `lib/sankoch.cyr` were stale, and all
seven are now fixed** (2026-08-11). An earlier pass of this review said "one
number", named two, and left the rest — an undercount that read as if the sweep
were done. The full set, against sankoch 2.7.7:

| citing | claimed | actual |
|---|---|---|
| `packaging.cyr:27` `_zip_path_safe` | `:14051` | 14082 |
| `packaging.cyr:27` read path | `:14541` | `_zip_prepare` 14631 |
| `packaging.cyr:54` DEFLATE→STORE | `:14795-14810` | `zip_add_meta` 14940 |
| `packaging.cyr:72` `zip_entry_size` | `:14470` | 14554 |
| `packaging.cyr:391` `zip_method_supported` | `:15138` | 15318 |
| `definitions_packaging.tcyr:136` | `:14774` | `zip_add_meta` 14940 |
| `definitions_packaging.tcyr:155` CD parse | `:14401` | `zip_open_a` 14372 |
| `definitions_packaging.tcyr:182` | `:14795-14810` | `zip_add_meta` 14940 |

⚠ **They are now cited by SYMBOL, not by line**, which is the fix Context
argues for and the only one that does not rot: every number above drifted when
sankoch grew, and re-pinning them would just reset the clock. A symbol survives
the fold; a line number is a hostage to it.
