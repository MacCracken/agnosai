# The image builds from src/ against a freshly synced lib/ (see Dockerfile), so
# none of this is read. rust-old/ alone is ~13 GB with its cargo target/, and
# without this file `COPY . .` sent all of it as build context.
rust-old/
build/
lib/
.git/
**/target/
