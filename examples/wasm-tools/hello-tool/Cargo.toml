[package]
name = "hello-tool"
description = "Example AgnosAI WASM tool — greets the user"
version = "1.1.0"
edition = "2024"
license = "GPL-3.0-only"

[dependencies]
agnosai-tool-sdk = { path = "../../../sdk/agnosai-tool-sdk" }
serde_json = "1"
