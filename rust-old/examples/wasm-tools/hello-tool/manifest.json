{
  "name": "hello",
  "description": "A greeting tool that says hello",
  "version": "1.0.0",
  "parameters": [
    {
      "name": "name",
      "description": "Who to greet (defaults to 'world')",
      "param_type": "string",
      "required": false
    }
  ]
}
